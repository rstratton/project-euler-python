This repo contains solutions, written in Python, to some of the problems listed
at [Project Euler](https://projecteuler.net/).

Running executables in bin
--------------------------

The executables in `bin` are meant to be run while the working directory is the
parent directory of `bin`; doing otherwise will cause errors.
